import React from 'react';
import PlayerForm from "../components/playerForm/PlayerForm";

const Form = () => {
    return (
        <main>
            <PlayerForm/>
        </main>
    );
};

export default Form;


